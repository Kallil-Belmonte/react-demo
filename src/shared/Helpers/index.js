import {
  setPageTitle,
  groupArrayItemsInArrays,
  removeItemsFromArray,
  setFieldClassName,
  getFieldErrorMessage,
} from './General';

import {
  capitalizeFirstLetter,
  capitalizeText,
  limitWords,
} from './Formatters';

export {
  // General
  setPageTitle,
  groupArrayItemsInArrays,
  removeItemsFromArray,
  setFieldClassName,
  getFieldErrorMessage,

  // Formatters
  capitalizeFirstLetter,
  capitalizeText,
  limitWords,
};
