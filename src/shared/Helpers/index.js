import {
  setPageTitle,
  groupArrayItemsInArrays,
  removeItemsFromArray,
  getFieldClassName,
  getFieldErrorMessage,
} from './General';

import {
  capitalizeFirstLetter,
  capitalizeText,
  limitWords,
} from './Formatters';

export {
  // General
  setPageTitle,
  groupArrayItemsInArrays,
  removeItemsFromArray,
  getFieldClassName,
  getFieldErrorMessage,

  // Formatters
  capitalizeFirstLetter,
  capitalizeText,
  limitWords,
};
