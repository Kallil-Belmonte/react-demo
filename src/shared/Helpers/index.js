import {
  setPageTitle,
  groupArrayItems,
  removeItemsFromIndexes,
  clearFormMessage,
} from './General';

import {
  capitalizeFirstLetter,
  capitalizeText,
  limitWords,
} from './Formatters';

export {
  // General
  setPageTitle,
  groupArrayItems,
  removeItemsFromIndexes,
  clearFormMessage,

  // Formatters
  capitalizeFirstLetter,
  capitalizeText,
  limitWords,
};
