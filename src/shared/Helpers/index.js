import {
  setPageTitle,
  groupArrayItemsInArrays,
  removeItemsFromIndexes,
  clearFormMessage,
} from './General';

import {
  capitalizeFirstLetter,
  capitalizeText,
  limitWords,
} from './Formatters';

export {
  // General
  setPageTitle,
  groupArrayItemsInArrays,
  removeItemsFromIndexes,
  clearFormMessage,

  // Formatters
  capitalizeFirstLetter,
  capitalizeText,
  limitWords,
};
